[project]
name = "scanner"
version = "0.1.0"
description = "Scans the mail infrastructure of DACH public-sector organisations and rates its digital sovereignty."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiodns>=4.0.0",
    "brotli>=1.2.0",
    "dnspython>=2.8.0",
    "ipwhois>=1.3.0",
    "ipykernel>=7.2.0",
    "matplotlib>=3.10.8",
    "overpy>=0.7",
    "pandas",
    "pyyaml",
    "requests",
    "scrapy>=2.16.0",
    "sqlalchemy",
    "tldextract>=5.3.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
]
